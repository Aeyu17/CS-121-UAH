#include <iostream> // input output stream
#include <fstream> // deals with files
#include <iomanip> // input output manipulator
#include <string> 
#include <math.h> // .h means that it's a C library, no .h means it's a C++ lib

using namespace std; // unnamed import, means you can do cout w/o std::cout

int main(void) {
	cout << "Hello world!" << endl;
	
	return 0;
}